# ── Variables de entorno del frontend ──

# URL base del backend (solo para producción)
# En desarrollo NO hace falta: el proxy de Vite redirige /api → localhost:3001
# En producción (Vercel) descomenta y rellena con tu URL de Render:
# VITE_API_BASE_URL=https://educandes-backend.onrender.com/api
